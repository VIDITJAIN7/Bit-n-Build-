# Copy this file to .env. The development launcher passes it only to the API.
WORKKITE_DATABASE=./data/averlock.db
WORKKITE_AGENT=rules
WORKKITE_WALLET=local
WORKKITE_AGENT_INTERVAL=5

# Optional AI provider. The adapter accepts OpenAI-compatible chat endpoints.
# Mistral starter (free mode has limited quotas; check its data controls):
# WORKKITE_AGENT=ai-risk
# WORKKITE_LLM_BASE_URL=https://api.mistral.ai/v1
# WORKKITE_LLM_MODEL=mistral-small-latest
# Paste your Mistral API key below. Keep it server-side.
# Gemini or another compatible provider? Change these three settings only.
WORKKITE_LLM_BASE_URL=
WORKKITE_LLM_MODEL=
WORKKITE_LLM_API_KEY=

# Local-only role routing; this does not secure API routes. Never use in production.
# Example: [{"email":"admin@company.local","password":"change-me","role":"admin"}]
WORKKITE_LOCAL_USERS_JSON=[]
# Keep provider, wallet, and identity secrets out of VITE_* variables.
