{
  "name": "workkite",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "contracts"
  ],
  "scripts": {
    "setup": "npm install && python scripts/setup.py",
    "dev": "node scripts/dev.mjs",
    "dev:chain": "node scripts/dev.mjs --chain",
    "dev:web": "npm run dev --workspace @workkite/web",
    "build": "npm run build --workspace @workkite/web",
    "test": "python scripts/test_api.py && npm run test --workspace @workkite/contracts",
    "test:api": "python scripts/test_api.py",
    "test:contracts": "npm run test --workspace @workkite/contracts"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "overrides": {
    "solc": {
      "tmp": "0.2.7"
    }
  }
}
